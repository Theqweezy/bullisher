<p id="notice"><%= notice %></p>

<div class="col-md-offset-2 col-md-8"><h1><small><strong><%=@idea.user.name%></strong> proposes: </small><%= @idea.description %></h1></div>

<br>
<br>
<%= render partial: "comments/comments", locals: {commentable: @idea} %>
<%= render partial: "comments/form", locals: {commentable: @idea} %>

<% if @idea.user == current_user %>
	<%= link_to 'Edit', edit_idea_path(@idea) %>
<% end %>
<%= link_to 'Back', ideas_path %>

